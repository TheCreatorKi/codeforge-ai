// server/index.js
console.log("CodeForge AI Server startet!");

// Beispiel: Mini-Funktion, die KI-Antwort simuliert
function generateCode(projectBrief) {
    // Später ersetzen wir das durch echten KI-Aufruf
    return `// Dein Mini-Code für Projekt: ${projectBrief}\nconsole.log("Hello World!");`;
}

// Beispiel-Aufruf
const demoProject = "Erstelle ein Todo App";
const result = generateCode(demoProject);
console.log("Generierter Code:\n", result);
